name: PitaTest

on: push

jobs:
  build_pita:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: setup
      run: |
        git submodule update --init --recursive
        sudo apt-get update -qq
        sudo apt-get install clang make cmake libboost-dev
        uname -a
        pwd
        clang++ -v
        ls -la
        mkdir build
        cd build
        cmake ..
        make -j
