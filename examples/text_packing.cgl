NotIntersectsMin = (a, b -> intersection(a, b) + Area(BoundingBox(a, b)))

main = Shape{
    a: text{
        str: "図"
    }
    b: text{
        str: "形"
    }
    sat(NotIntersectsMin(a, b))
    var(b.pos)
}